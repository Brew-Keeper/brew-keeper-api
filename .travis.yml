language: python
python:
  - "3.4.3"
env:
- PYTHONPATH=api
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests

before_script:
  python brewkeeper/manage.py runserver &

script:
  run brewkeeper/manage.py test
# coverage run functional_tests.py
